#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mithril/ospec/bin/ospec" "$@"
  ret=$?
else 
  node  "$basedir/../mithril/ospec/bin/ospec" "$@"
  ret=$?
fi
exit $ret
